implementation
var
    a, b: Integer;
    c, d: Single;

initialization
    a := 1;
    b := 2;
    c := 0.5;
    d := 1.5;

    WriteLn('a = ' + a);
    WriteLn('b = ' + b);
    WriteLn('c = ' + c);
    WriteLn('d = ' + d);
end
